// Generated by CoffeeScript 1.3.3
(function() {
  var defineNamespace;

  defineNamespace = function(defaultContext) {
    var _namespace;
    if (defaultContext == null) {
      defaultContext = this;
    }
    return _namespace = function(ns, context, func) {
      var chain, name;
      if (!(func != null)) {
        func = context;
        context = defaultContext;
      }
      chain = ns.split('.');
      name = chain.shift();
      if (!(context[name] != null)) {
        context[name] = {};
      } else if (!(context[name] != null) || (context[name].toString() !== '[object Object]')) {
        throw new Error("Namespace part '" + name + "' can not be created");
      }
      if (chain.length === 0) {
        return func.call(context[name]);
      } else {
        return _namespace(chain.join('.'), context[name], func);
      }
    };
  };

  if ((typeof module !== "undefined" && module !== null) && (module.exports != null)) {
    module.exports = defineNamespace(global);
  } else {
    this.namespace = defineNamespace(this);
  }

}).call(this);
